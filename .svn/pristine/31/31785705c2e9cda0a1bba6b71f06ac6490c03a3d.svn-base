var express = require('express');
var router = express.Router();
var path = require('path');
var utils = require("../utils");

/* GET home page. */
router.get('/rgb', function(req, res, next) {
    res.render("tool/rgb",{title:"RGB 调色工具，颜色转换"});
});

module.exports = router;

var express = require('express');
var router = express.Router();
var path = require('path');

/* GET home page. */
router.get('/', function(req, res, next) {
  res.render('learn', { title: '学无止境,想学或者推荐的有意思的东西' });
});

module.exports = router;
